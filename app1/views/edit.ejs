<!DOCTYPE html>
<html lang="ja">

<head>
    <meta charset="UTF-8">
    <title>編集</title>
</head>

<body>
    <h1>編集</h1>

    <form action="/formula/update/<%= item.id %>" method="POST">
        <div>
            <label>名前:</label><br>
            <input type="text" name="name" value="<%= item.name %>" required>
        </div>
        <br>
        <div>
            <label>説明:</label><br>
            <textarea name="description" rows="5" cols="30"><%= item.description %></textarea>
        </div>
        <br>
        <button type="submit">更新</button>
    </form>

    <hr>
    <a href="/formula">キャンセルして一覧に戻る</a>
</body>

</html>

